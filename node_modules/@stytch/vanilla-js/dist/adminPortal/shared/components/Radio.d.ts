/// <reference types="react" />
export type OptionItem = {
    disabled?: boolean;
    label: string;
    value: string;
};
export type RadioCoreProps = OptionItem & {
    /**
     * Whether to autofocus the radio
     */
    autoFocus?: boolean;
    /**
     * Whether the checkbox is checked
     */
    checked: boolean;
    className?: string;
    /**
     * Function to run when the radio button is clicked
     */
    onClick?: (value: string) => void;
};
export declare const RadioCore: ({ autoFocus, checked, className, disabled, label, onClick, value, }: RadioCoreProps) => JSX.Element;
