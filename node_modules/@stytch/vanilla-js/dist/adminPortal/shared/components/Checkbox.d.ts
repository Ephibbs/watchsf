import { SxProps, Theme } from '@mui/material';
import { ReactNode } from 'react';
import { OptionItem } from './types';
export type CheckboxCoreProps = Omit<OptionItem, 'value' | 'label'> & {
    autoFocus?: boolean;
    checked?: boolean;
    className?: string;
    indeterminate?: boolean;
    label: ReactNode;
    onClick?: (checked: boolean, value: string) => void;
    value?: string;
    checkboxSx?: SxProps<Theme>;
};
export declare const CheckboxCore: ({ autoFocus, checkboxSx, checked, className, disabled, indeterminate, label, onClick, value, }: CheckboxCoreProps) => JSX.Element;
