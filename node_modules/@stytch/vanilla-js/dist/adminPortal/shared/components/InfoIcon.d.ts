/// <reference types="react" />
import { InjectedComponents } from './componentInjection';
export type InfoIconCoreProps = {
    tooltipText: string;
    color?: string;
};
export declare const InfoIconCore: ({ tooltipText, color, TooltipComponent: Tooltip, }: InfoIconCoreProps & InjectedComponents<'Tooltip'>) => JSX.Element;
