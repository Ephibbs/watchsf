import { TooltipProps } from '@mui/material';
import { ReactElement } from 'react';
import { InjectedComponents } from './componentInjection';
export type TooltipCoreProps = {
    children: ReactElement;
    text: string;
    disabled?: boolean;
    open?: boolean;
    placement?: TooltipProps['placement'];
};
export declare const TooltipCore: ({ text, children, disabled, open, placement, TypographyComponent: Typography, }: TooltipCoreProps & InjectedComponents<'Typography'>) => JSX.Element;
