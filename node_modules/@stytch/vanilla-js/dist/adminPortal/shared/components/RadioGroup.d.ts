/// <reference types="react" />
import { SxProps, Theme } from '@mui/material';
import { OptionItem } from './Radio';
import { InjectedComponents } from './componentInjection';
export type RadioGroupCoreProps = {
    id?: string;
    items: OptionItem[];
    onChange?(value: string): void;
    readOnly?: boolean;
    value?: string;
    formGroupSx?: SxProps<Theme>;
};
export declare const RadioGroupCore: ({ id, items, onChange, readOnly, value, formGroupSx, RadioComponent: Radio, }: RadioGroupCoreProps & InjectedComponents<'Radio'>) => JSX.Element;
