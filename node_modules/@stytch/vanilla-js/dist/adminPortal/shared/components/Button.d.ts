import { ButtonProps as InternalButtonProps } from '@mui/material';
import { FC, MouseEventHandler, ReactNode } from 'react';
export type Variant = 'ghost' | 'primary' | 'text';
export type ButtonCoreProps = {
    autoFocus?: boolean;
    children?: ReactNode;
    compact?: boolean;
    component?: 'span' | 'button' | 'div' | 'label';
    disabled?: boolean;
    endIcon?: ReactNode;
    fullWidth?: boolean;
    onClick?: MouseEventHandler<HTMLButtonElement>;
    startIcon?: ReactNode;
    type?: InternalButtonProps['type'];
    variant?: Variant;
    warning?: boolean;
    /**
     * Should not be used directly! This is required for styled(Button) to work.
     */
    className?: string;
};
export declare const ButtonCore: FC<ButtonCoreProps>;
