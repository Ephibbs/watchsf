import { FC } from 'react';
import { InjectedComponents } from './componentInjection';
import { SxProps, Theme } from '@mui/material';
export type CopyableTextCoreProps = {
    children?: string;
    textToCopy?: string;
    noWrap?: boolean;
    id?: string;
    label?: string;
    wrapperSx?: SxProps<Theme>;
};
export declare const CopyableTextCore: ({ children, textToCopy, noWrap, id, label, wrapperSx, CodeComponent: Code, LabelComponent: Label, TypographyComponent: Typography, useClickToCopy, }: CopyableTextCoreProps & InjectedComponents<'Code' | 'Label' | 'Typography' | 'useClickToCopy'>) => JSX.Element;
export interface CopyableTruncatedIdCoreProps {
    children: string;
}
export declare const CopyableTruncatedIdCore: FC<CopyableTruncatedIdCoreProps & InjectedComponents<'CopyableText'>>;
